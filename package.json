{
  "name": "seo4ajax-monitor",
  "version": "1.0.0",
  "description": "Little script to get the pending captures on multiple seo4ajax sites",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async": "^0.9.0",
    "request": "^2.46.0"
  }
}
